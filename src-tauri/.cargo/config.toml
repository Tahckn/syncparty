# ts-rs writes the generated TypeScript definitions straight into the frontend
# source tree, so `shared/types` is never hand-maintained. Regenerate with
# `cargo test export_bindings`.
[env]
TS_RS_EXPORT_DIR = { value = "../src/shared/types", relative = true }
